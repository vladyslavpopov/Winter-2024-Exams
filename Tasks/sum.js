// Sum all numbers from an array

const k = (sum = function (s = () => {}) {
  
  for (i of s) {
    if (t === 'number') {
       
    }
  }
  
  return sum[sum.length - 1];
});

module.exports = sum;
