// Sum all numbers from an array

const k = (sum = function (s = () => {}) {
  let result = 0;
  for (i of s) {
    if (t === 'number') {
       
    }
  }
  
  return result;
});

module.exports = sum;
